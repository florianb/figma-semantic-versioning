{
  "name": "Butterfly",
  "version": "1.0.0",
  "description": "Your Figma plugin",
  "main": "code.js",
  "scripts": {
    "lint-watch": "watchexec -i dist npm run lint",
    "lint": "tsc -noEmit -p tsconfig.json && xo **/*.ts",
    "build": "esbuild code.ts ui.ts --bundle --outdir=dist",
    "watch": "esbuild code.ts ui.ts --watch --bundle --outdir=dist"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "^1.39.1",
    "alpinejs": "^3.5.2",
    "ava": "^3.15.0",
    "esbuild": "^0.14.0",
    "typescript": "^4.5.2",
    "xo": "^0.47.0"
  }
}
